/* Core */
import { FC } from 'react';
// /* Components */
import { Profile } from '../components/Profile';

export const ProfilePage: FC = () => {
    return (
        <main>
            <Profile />
        </main>
    );
};
