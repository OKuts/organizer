/* Core */
import { FC } from 'react';

// /* Components */
import { SignUpForm } from '../components/forms';

export const SignUpPage: FC = () => {
    return (
        <main>
            <SignUpForm />
        </main>
    );
};
